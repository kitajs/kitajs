{
  "name": "@kitajs/monorepo",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://github.com/arthurfiorette/kita#readme",
  "bugs": {
    "url": "https://github.com/arthurfiorette/kita/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arthurfiorette/kita.git"
  },
  "license": "MIT",
  "author": "Arthur Fiorette <kita@arthur.place>",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "change": "changeset",
    "ci:publish": "pnpm publish -r --access public && changeset publish",
    "ci:version": "changeset version && pnpm install --no-frozen-lockfile",
    "docs": "cd packages/docs && pnpm dev",
    "format": "prettier --write .",
    "preinstall": "npx only-allow pnpm",
    "prepare": "husky install",
    "test": "pnpm -r --parallel test",
    "watch": "pnpm -r --parallel --no-bail build --watch "
  },
  "devDependencies": {
    "@arthurfiorette/prettier-config": "^1.0.11",
    "@changesets/cli": "^2.26.2",
    "@kitajs/ts-plugin": "workspace:^",
    "husky": "^8.0.3",
    "prettier": "^3.0.3",
    "typescript": "~5.1.6"
  },
  "packageManager": "pnpm@8.8.0",
  "engines": {
    "node": ">=20.8.0",
    "pnpm": ">=8.8.0"
  }
}
